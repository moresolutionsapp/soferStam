<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


        <item
            android:id="@+id/action_last_file"
            android:enabled="true"
            android:title="המשך עבודה"
            android:checked="false"
            app:showAsAction="always"
            android:visible="true"
            android:icon="@drawable/ic_last_page_24"/>

        <item
            android:id="@+id/action_image_size"
            android:enabled="true"
            android:title="גודל עמודה (1)"
            app:showAsAction="always"
            android:checked="false"
            android:visible="true"
            android:icon="@drawable/ic_text_size_24"/>


        <item
            android:id="@+id/action_screen_size"
            android:enabled="true"
            android:title="גודל מסך"
            app:showAsAction="always"
            android:visible="false"
            android:icon="@drawable/ic_upload_24"/>










</menu>